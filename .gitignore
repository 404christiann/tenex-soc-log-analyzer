# dependencies
node_modules/
.pnp
.pnp.js

# env
.env
.env.local
.env.*.local

# next.js
.next/
out/

# build output
dist/
build/

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# example synthetic SOC log fixtures are a deliverable, not runtime output — keep them tracked
!examples/*.log

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# vercel
.vercel

# supabase local
supabase/.branches
supabase/.temp

# playwright (apps/web/e2e — DECISIONS.md §14b)
apps/web/test-results
apps/web/playwright-report
apps/web/blob-report
apps/web/playwright/.cache

# design-exploration git worktrees (not part of the deliverable)
.claude/worktrees
.env*

# Claude Code local dev-server config (references local ports only, but no
# reason to track it as part of the deliverable)
.claude/launch.json

# auto-generated by `next dev` (generate-agent-files.js) on every run — not
# meant to be tracked, and exposes build-process internals in the submission
apps/web/AGENTS.md
apps/web/CLAUDE.md

# local working docs (kept on disk, not part of the published repo)
/HANDOFF.md

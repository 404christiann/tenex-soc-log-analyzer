{
  "name": "@tenex/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas and shared types for the log event shape and API request/response contracts, consumed as raw TypeScript by apps/web and apps/api (no build step).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
